<h1>Homepage</h1>

<h3>Randomly Selected City</h3>
<% random_city = City.all.sample %>

<% if random_city %>
  <p>Name: <%= random_city.name %></p>
  <p>Population: <%= random_city.population %></p>
<% else %>
  <p>No cities found.</p>
<% end %>

<%= line_chart User.group_by_day(:created_at).count %>

<%= link_to "User Sign In", users_path %>

<%= render partial: 'pages/modal' %>
